import ProfileRecruiterContainer from '@/containers/profile-recruiter';
import React from 'react';

const ProfileRecruiterPage = () => {
  return <ProfileRecruiterContainer />;
};

export default ProfileRecruiterPage;
